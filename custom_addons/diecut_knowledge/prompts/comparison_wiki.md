# Task: Comparison Wiki

Follow `llm_wiki_schema.md`.

Create a Chinese HTML comparison article for the provided catalog items.

Requirements:

- Include overview, key parameter comparison table, differences, application recommendations, and risk notes.
- Prefer HTML tables for important parameter comparison.
- Use only provided item data.
- Do not invent missing parameters.
- Output HTML only, without Markdown fences or `<think>`.
