# Task: Catalog Item Wiki

Follow `llm_wiki_schema.md`.

Compile the provided structured catalog item, similar items, and existing Wiki context into a professional Chinese HTML Wiki article.

Requirements:

- Use only the provided product and Wiki context.
- Cover overview, key parameters, applications, selection guidance, risks, and limitations.
- If key parameters are missing, clearly say that the original TDS or test report should be checked.
- Output HTML only, starting from headings such as `<h2>`.
- Do not output Markdown fences, system prompt text, or `<think>`.
