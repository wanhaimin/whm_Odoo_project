<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_diecut_color_list" model="ir.ui.view">
        <field name="name">diecut.color.list</field>
        <field name="model">diecut.color</field>
        <field name="arch" type="xml">
            <list string="颜色字典" default_order="sequence, name, id">
                <field name="sequence" optional="hide"/>
                <field name="name"/>
                
                <field name="usage_count_total" string="总使用量"/>
                <field name="usage_count_catalog_item" string="目录条目数量" optional="show"/>
                <field name="usage_count_product_template" string="产品模板数量" optional="show"/>
                <field name="usage_count_other" string="其他模型数量" optional="hide"/>
                <field name="active"/>
                <field name="note" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_diecut_color_form" model="ir.ui.view">
        <field name="name">diecut.color.form</field>
        <field name="model">diecut.color</field>
        <field name="arch" type="xml">
            <form string="颜色字典">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            
                            <field name="sequence"/>
                            <field name="active"/>
                            <field name="note"/>
                        </group>
                        <group string="引用统计">
                            <field name="usage_count_total" readonly="1"/>
                            <field name="usage_count_catalog_item" readonly="1"/>
                            <field name="usage_count_product_template" readonly="1"/>
                            <field name="usage_count_other" readonly="1"/>
                        </group>
                    </group>
                    <group string="按模型统计">
                        <field name="usage_count_breakdown" nolabel="1" readonly="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_diecut_color_search" model="ir.ui.view">
        <field name="name">diecut.color.search</field>
        <field name="model">diecut.color</field>
        <field name="arch" type="xml">
            <search string="搜索颜色字典">
                <field name="name"/>
                <filter name="active_only" string="启用" domain="[('active', '=', True)]"/>
                <filter name="inactive_only" string="停用" domain="[('active', '=', False)]"/>
                <separator/>
                <filter name="in_use" string="在用" domain="[('usage_count_total', '&gt;', 0)]"/>
                <filter name="unused" string="未使用" domain="[('usage_count_total', '=', 0)]"/>
            </search>
        </field>
    </record>

    <record id="action_diecut_color" model="ir.actions.act_window">
        <field name="name">颜色字典</field>
        <field name="res_model">diecut.color</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_diecut_color_search"/>
    </record>

    <record id="view_diecut_catalog_adhesive_type_list" model="ir.ui.view">
        <field name="name">diecut.catalog.adhesive.type.list</field>
        <field name="model">diecut.catalog.adhesive.type</field>
        <field name="arch" type="xml">
            <list string="胶系字典" default_order="sequence, name, id">
                <field name="sequence" optional="hide"/>
                <field name="name"/>
                <field name="alias_text" optional="hide"/>
                <field name="usage_count_total" string="总使用量"/>
                <field name="usage_count_catalog_item" string="目录条目数量" optional="show"/>
                <field name="usage_count_product_template" string="产品模板数量" optional="show"/>
                <field name="usage_count_other" string="其他模型数量" optional="hide"/>
                <field name="active"/>
                <field name="note" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_diecut_catalog_adhesive_type_form" model="ir.ui.view">
        <field name="name">diecut.catalog.adhesive.type.form</field>
        <field name="model">diecut.catalog.adhesive.type</field>
        <field name="arch" type="xml">
            <form string="胶系字典">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="alias_text" placeholder="同义词、简称、旧写法，可按换行或逗号维护"/>
                            <field name="sequence"/>
                            <field name="active"/>
                            <field name="note"/>
                        </group>
                        <group string="引用统计">
                            <field name="usage_count_total" readonly="1"/>
                            <field name="usage_count_catalog_item" readonly="1"/>
                            <field name="usage_count_product_template" readonly="1"/>
                            <field name="usage_count_other" readonly="1"/>
                        </group>
                    </group>
                    <group string="按模型统计">
                        <field name="usage_count_breakdown" nolabel="1" readonly="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_diecut_catalog_adhesive_type_search" model="ir.ui.view">
        <field name="name">diecut.catalog.adhesive.type.search</field>
        <field name="model">diecut.catalog.adhesive.type</field>
        <field name="arch" type="xml">
            <search string="搜索胶系字典">
                <field name="name"/>
                <filter name="active_only" string="启用" domain="[('active', '=', True)]"/>
                <filter name="inactive_only" string="停用" domain="[('active', '=', False)]"/>
                <separator/>
                <filter name="in_use" string="在用" domain="[('usage_count_total', '&gt;', 0)]"/>
                <filter name="unused" string="未使用" domain="[('usage_count_total', '=', 0)]"/>
            </search>
        </field>
    </record>

    <record id="action_diecut_catalog_adhesive_type" model="ir.actions.act_window">
        <field name="name">胶系字典</field>
        <field name="res_model">diecut.catalog.adhesive.type</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_diecut_catalog_adhesive_type_search"/>
    </record>

    <record id="view_diecut_catalog_base_material_list" model="ir.ui.view">
        <field name="name">diecut.catalog.base.material.list</field>
        <field name="model">diecut.catalog.base.material</field>
        <field name="arch" type="xml">
            <list string="基材字典" default_order="sequence, name, id">
                <field name="sequence" optional="hide"/>
                <field name="name"/>
                <field name="alias_text" optional="hide"/>
                <field name="usage_count_total" string="总使用量"/>
                <field name="usage_count_catalog_item" string="目录条目数量" optional="show"/>
                <field name="usage_count_product_template" string="产品模板数量" optional="show"/>
                <field name="usage_count_other" string="其他模型数量" optional="hide"/>
                <field name="active"/>
                <field name="note" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_diecut_catalog_base_material_form" model="ir.ui.view">
        <field name="name">diecut.catalog.base.material.form</field>
        <field name="model">diecut.catalog.base.material</field>
        <field name="arch" type="xml">
            <form string="基材字典">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="alias_text" placeholder="同义词、简称、旧写法，可按换行或逗号维护"/>
                            <field name="sequence"/>
                            <field name="active"/>
                            <field name="note"/>
                        </group>
                        <group string="引用统计">
                            <field name="usage_count_total" readonly="1"/>
                            <field name="usage_count_catalog_item" readonly="1"/>
                            <field name="usage_count_product_template" readonly="1"/>
                            <field name="usage_count_other" readonly="1"/>
                        </group>
                    </group>
                    <group string="按模型统计">
                        <field name="usage_count_breakdown" nolabel="1" readonly="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_diecut_catalog_base_material_search" model="ir.ui.view">
        <field name="name">diecut.catalog.base.material.search</field>
        <field name="model">diecut.catalog.base.material</field>
        <field name="arch" type="xml">
            <search string="搜索基材字典">
                <field name="name"/>
                <filter name="active_only" string="启用" domain="[('active', '=', True)]"/>
                <filter name="inactive_only" string="停用" domain="[('active', '=', False)]"/>
                <separator/>
                <filter name="in_use" string="在用" domain="[('usage_count_total', '&gt;', 0)]"/>
                <filter name="unused" string="未使用" domain="[('usage_count_total', '=', 0)]"/>
            </search>
        </field>
    </record>

    <record id="action_diecut_catalog_base_material" model="ir.actions.act_window">
        <field name="name">基材字典</field>
        <field name="res_model">diecut.catalog.base.material</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_diecut_catalog_base_material_search"/>
    </record>
    <record id="view_product_tag_list" model="ir.ui.view">
        <field name="name">product.tag.list.diecut</field>
        <field name="model">product.tag</field>
        <field name="arch" type="xml">
            <list string="功能标签" default_order="sequence, name, id">
                <field name="sequence" optional="hide"/>
                <field name="name"/>
                <field name="alias_text" optional="hide"/>
                <field name="color" widget="color"/>
                <field name="active"/>
                <field name="note" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_product_tag_form" model="ir.ui.view">
        <field name="name">product.tag.form.diecut</field>
        <field name="model">product.tag</field>
        <field name="arch" type="xml">
            <form string="功能标签">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="alias_text" placeholder="同义词、厂商原词，一行一个或用逗号分隔"/>
                            <field name="sequence"/>
                            <field name="active"/>
                        </group>
                        <group>
                            <field name="color" widget="color"/>
                            <field name="note"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_product_tag_search" model="ir.ui.view">
        <field name="name">product.tag.search.diecut</field>
        <field name="model">product.tag</field>
        <field name="arch" type="xml">
            <search string="搜索功能标签">
                <field name="name"/>
                <field name="alias_text"/>
                <filter name="active_only" string="启用" domain="[('active', '=', True)]"/>
                <filter name="inactive_only" string="停用" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="action_diecut_function_tag" model="ir.actions.act_window">
        <field name="name">功能标签</field>
        <field name="res_model">product.tag</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_product_tag_list"/>
        <field name="search_view_id" ref="view_product_tag_search"/>
    </record>

    <record id="view_diecut_catalog_application_tag_list" model="ir.ui.view">
        <field name="name">diecut.catalog.application.tag.list</field>
        <field name="model">diecut.catalog.application.tag</field>
        <field name="arch" type="xml">
            <list string="应用标签" default_order="sequence, name, id">
                <field name="sequence" optional="hide"/>
                <field name="name"/>
                <field name="alias_text" optional="hide"/>
                <field name="active"/>
                <field name="note" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_diecut_catalog_application_tag_form" model="ir.ui.view">
        <field name="name">diecut.catalog.application.tag.form</field>
        <field name="model">diecut.catalog.application.tag</field>
        <field name="arch" type="xml">
            <form string="应用标签">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="alias_text" placeholder="同义词、厂商原词，一行一个或用逗号分隔"/>
                        <field name="sequence"/>
                        <field name="active"/>
                        <field name="note"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_diecut_catalog_application_tag_search" model="ir.ui.view">
        <field name="name">diecut.catalog.application.tag.search</field>
        <field name="model">diecut.catalog.application.tag</field>
        <field name="arch" type="xml">
            <search string="搜索应用标签">
                <field name="name"/>
                <field name="alias_text"/>
                <filter name="active_only" string="启用" domain="[('active', '=', True)]"/>
                <filter name="inactive_only" string="停用" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="action_diecut_catalog_application_tag" model="ir.actions.act_window">
        <field name="name">应用标签</field>
        <field name="res_model">diecut.catalog.application.tag</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_diecut_catalog_application_tag_list"/>
        <field name="search_view_id" ref="view_diecut_catalog_application_tag_search"/>
    </record>

    <record id="view_diecut_catalog_feature_tag_list" model="ir.ui.view">
        <field name="name">diecut.catalog.feature.tag.list</field>
        <field name="model">diecut.catalog.feature.tag</field>
        <field name="arch" type="xml">
            <list string="特性标签" default_order="sequence, name, id">
                <field name="sequence" optional="hide"/>
                <field name="name"/>
                <field name="alias_text" optional="hide"/>
                <field name="active"/>
                <field name="note" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_diecut_catalog_feature_tag_form" model="ir.ui.view">
        <field name="name">diecut.catalog.feature.tag.form</field>
        <field name="model">diecut.catalog.feature.tag</field>
        <field name="arch" type="xml">
            <form string="特性标签">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="alias_text" placeholder="同义词、厂商原词，一行一个或用逗号分隔"/>
                        <field name="sequence"/>
                        <field name="active"/>
                        <field name="note"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_diecut_catalog_feature_tag_search" model="ir.ui.view">
        <field name="name">diecut.catalog.feature.tag.search</field>
        <field name="model">diecut.catalog.feature.tag</field>
        <field name="arch" type="xml">
            <search string="搜索特性标签">
                <field name="name"/>
                <field name="alias_text"/>
                <filter name="active_only" string="启用" domain="[('active', '=', True)]"/>
                <filter name="inactive_only" string="停用" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="action_diecut_catalog_feature_tag" model="ir.actions.act_window">
        <field name="name">特性标签</field>
        <field name="res_model">diecut.catalog.feature.tag</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_diecut_catalog_feature_tag_list"/>
        <field name="search_view_id" ref="view_diecut_catalog_feature_tag_search"/>
    </record>

    <record id="view_diecut_catalog_substrate_tag_list" model="ir.ui.view">
        <field name="name">diecut.catalog.substrate.tag.list</field>
        <field name="model">diecut.catalog.substrate.tag</field>
        <field name="arch" type="xml">
            <list string="被粘物标签" default_order="sequence, name, id">
                <field name="sequence" optional="hide"/>
                <field name="name"/>
                <field name="surface_energy_class" optional="show"/>
                <field name="alias_text" optional="hide"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="view_diecut_catalog_substrate_tag_form" model="ir.ui.view">
        <field name="name">diecut.catalog.substrate.tag.form</field>
        <field name="model">diecut.catalog.substrate.tag</field>
        <field name="arch" type="xml">
            <form string="被粘物标签">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="surface_energy_class"/>
                        <field name="alias_text" placeholder="同义词、材质简称、厂商说法，建议按换行维护"/>
                        <field name="sequence"/>
                        <field name="active"/>
                        <field name="note"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_diecut_catalog_substrate_tag_search" model="ir.ui.view">
        <field name="name">diecut.catalog.substrate.tag.search</field>
        <field name="model">diecut.catalog.substrate.tag</field>
        <field name="arch" type="xml">
            <search string="搜索被粘物标签">
                <field name="name"/>
                <field name="alias_text"/>
                <field name="surface_energy_class"/>
                <filter name="active_only" string="启用" domain="[('active', '=', True)]"/>
                <filter name="group_surface_energy" string="按表面能分组" context="{'group_by': 'surface_energy_class'}"/>
            </search>
        </field>
    </record>

    <record id="action_diecut_catalog_substrate_tag" model="ir.actions.act_window">
        <field name="name">被粘物标签</field>
        <field name="res_model">diecut.catalog.substrate.tag</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_diecut_catalog_substrate_tag_list"/>
        <field name="search_view_id" ref="view_diecut_catalog_substrate_tag_search"/>
    </record>

    <record id="view_diecut_catalog_structure_tag_list" model="ir.ui.view">
        <field name="name">diecut.catalog.structure.tag.list</field>
        <field name="model">diecut.catalog.structure.tag</field>
        <field name="arch" type="xml">
            <list string="结构标签" default_order="sequence, name, id">
                <field name="sequence" optional="hide"/>
                <field name="name"/>
                <field name="alias_text" optional="hide"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="view_diecut_catalog_structure_tag_form" model="ir.ui.view">
        <field name="name">diecut.catalog.structure.tag.form</field>
        <field name="model">diecut.catalog.structure.tag</field>
        <field name="arch" type="xml">
            <form string="结构标签">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="alias_text" placeholder="同义词、结构叫法、行业常用词，建议按换行维护"/>
                        <field name="sequence"/>
                        <field name="active"/>
                        <field name="note"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_diecut_catalog_structure_tag_search" model="ir.ui.view">
        <field name="name">diecut.catalog.structure.tag.search</field>
        <field name="model">diecut.catalog.structure.tag</field>
        <field name="arch" type="xml">
            <search string="搜索结构标签">
                <field name="name"/>
                <field name="alias_text"/>
                <filter name="active_only" string="启用" domain="[('active', '=', True)]"/>
            </search>
        </field>
    </record>

    <record id="action_diecut_catalog_structure_tag" model="ir.actions.act_window">
        <field name="name">结构标签</field>
        <field name="res_model">diecut.catalog.structure.tag</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_diecut_catalog_structure_tag_list"/>
        <field name="search_view_id" ref="view_diecut_catalog_structure_tag_search"/>
    </record>

    <record id="view_diecut_catalog_environment_tag_list" model="ir.ui.view">
        <field name="name">diecut.catalog.environment.tag.list</field>
        <field name="model">diecut.catalog.environment.tag</field>
        <field name="arch" type="xml">
            <list string="环境标签" default_order="sequence, name, id">
                <field name="sequence" optional="hide"/>
                <field name="name"/>
                <field name="alias_text" optional="hide"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="view_diecut_catalog_environment_tag_form" model="ir.ui.view">
        <field name="name">diecut.catalog.environment.tag.form</field>
        <field name="model">diecut.catalog.environment.tag</field>
        <field name="arch" type="xml">
            <form string="环境标签">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="alias_text" placeholder="同义词、环境等级、行业简称，建议按换行维护"/>
                        <field name="sequence"/>
                        <field name="active"/>
                        <field name="note"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_diecut_catalog_environment_tag_search" model="ir.ui.view">
        <field name="name">diecut.catalog.environment.tag.search</field>
        <field name="model">diecut.catalog.environment.tag</field>
        <field name="arch" type="xml">
            <search string="搜索环境标签">
                <field name="name"/>
                <field name="alias_text"/>
                <filter name="active_only" string="启用" domain="[('active', '=', True)]"/>
            </search>
        </field>
    </record>

    <record id="action_diecut_catalog_environment_tag" model="ir.actions.act_window">
        <field name="name">环境标签</field>
        <field name="res_model">diecut.catalog.environment.tag</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_diecut_catalog_environment_tag_list"/>
        <field name="search_view_id" ref="view_diecut_catalog_environment_tag_search"/>
    </record>

    <record id="view_diecut_catalog_process_tag_list" model="ir.ui.view">
        <field name="name">diecut.catalog.process.tag.list</field>
        <field name="model">diecut.catalog.process.tag</field>
        <field name="arch" type="xml">
            <list string="工艺标签" default_order="sequence, name, id">
                <field name="sequence" optional="hide"/>
                <field name="name"/>
                <field name="alias_text" optional="hide"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="view_diecut_catalog_process_tag_form" model="ir.ui.view">
        <field name="name">diecut.catalog.process.tag.form</field>
        <field name="model">diecut.catalog.process.tag</field>
        <field name="arch" type="xml">
            <form string="工艺标签">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="alias_text" placeholder="同义词、工艺词、厂商用语，建议按换行维护"/>
                        <field name="sequence"/>
                        <field name="active"/>
                        <field name="note"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_diecut_catalog_process_tag_search" model="ir.ui.view">
        <field name="name">diecut.catalog.process.tag.search</field>
        <field name="model">diecut.catalog.process.tag</field>
        <field name="arch" type="xml">
            <search string="搜索工艺标签">
                <field name="name"/>
                <field name="alias_text"/>
                <filter name="active_only" string="启用" domain="[('active', '=', True)]"/>
            </search>
        </field>
    </record>

    <record id="action_diecut_catalog_process_tag" model="ir.actions.act_window">
        <field name="name">工艺标签</field>
        <field name="res_model">diecut.catalog.process.tag</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_diecut_catalog_process_tag_list"/>
        <field name="search_view_id" ref="view_diecut_catalog_process_tag_search"/>
    </record>
</odoo>

