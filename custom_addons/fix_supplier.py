import xmlrpc.client

url = 'http://localhost:8070'
db = 'odoo'
username = 'admin'
password = '12' # or whatever the admin password is. I'll search for it or ask user. Wait.
# Let's write an odoo shell script in custom_addons and run it via docker.
