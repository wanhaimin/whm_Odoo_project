通过检查代码，我找到了原因！

这些数据确实已经作为代码的一部分存在于你的项目中了，所以它们才能通过 GitHub 自动同步。

在此处找到：

文件位置: 
custom_addons/my_material_list/data/my.material.category.csv
所属模块: my_material_list （你的 diecut_custom 模块依赖于它）
证据分析
在 my_material_list 模块中，有一份 CSV 文件（一种类似 Excel 的表格格式），里面完整地记录了你截图中的所有数据：
第 9 行: category_7,三层防刮花硬化膜
第 10 行: category_8,铝箔
第 24 行: category_22,3M
...以及其他 60 多条记录。
在 
my_material_list/
manifest
.py
 第 15 行：
python
'data/my.material.category.csv',
这行代码指示 Odoo 在安装或升级该模块时，自动读取这个 CSV 文件并写入数据库。
结论
不需要手动录入，也不需要复制数据库。因为这些数据已经写在 
csv
 文件里了，所以它们本质上就是代码。

当你使用 GitHub 同步代码到另一台电脑，并安装 diecut_custom（它会自动安装依赖 my_material_list）时，Odoo 就会自动把这些 CSV 里的数据加载进去。

这就是为什么你什么都没做，数据却同步了的原因。这是一个非常好的实践！ 建议后续的“材质规格”等基础数据也采用这种方式管理。