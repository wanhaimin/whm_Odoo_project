1，报价
  成本计算模型已经建立，需要更详细的设计   负责部门-工程部
  报检单模型尚未建立核心  根据成本核算模型计算的结果，生成给客户的报价。（list form）
  
  原材料模型（核心），odoo原生product.template,  product.product（产品变体）管理？？
  销售产品模型（核心）
  
2，打样
  样品管理管理模型
  
3，BOM
4，销售订单
   产品核心字段

5，采购
6, 生产
  刀模管理模型
  排产模型
  生产日报模型
  
7，仓库

产品数据库创建

分类


## 列表对齐方式
[[2026-02-07]]


Odoo 的列表视图（List/Tree View）对齐方式设置非常灵活，主要通过在 XML 中添加 CSS 类来实现。以下是详细的设置教程：

### 1. 核心对齐类 (CSS Classes)

Odoo 使用标准的 Bootstrap 风格类名来控制对齐。在 

 属性即可：

|对齐方式|类名 (推荐用新版)|旧版写法 (兼容)|适用场景|
|---|---|---|---|
|**左对齐**|```<br>class="text-start"<br>```|```<br>class="text-left"<br>```|文本、名称、描述|
|**居中对齐**|```<br>class="text-center"<br>```|```<br>class="text-center"<br>```|状态、短代码、布尔值、图片|
|**右对齐**|```<br>class="text-end"<br>```|```<br>class="text-right"<br>```|金额、数量、数字|

### 2. 如何设置 (代码示例)



**注意：** Odoo 的设计逻辑是 **“列对齐”**。也就是说，当您设置了字段的对齐方式，**表头 (Header) 会自动跟随内容的对齐方式**。

#### 示例场景：

### 3. 表头对齐的特殊说明

- **默认行为**：表头的对齐方式默认与该列的内容对齐方式一致。
    - 内容居中 -> 表头居中
    - 内容右对齐 -> 表头右对齐
- **能否“表头居中，内容居左”？**
    - **标准 XML 不支持**这种直接配置。这是 Odoo 为了保持视觉整洁做的限制。
    - 如果必须实现“表头居中，内容居左”，通常需要写自定义 CSS 或 JS，或者牺牲一下让内容也居中（对于短文本通常可以接受）。

### 4. 其他相关技巧

- 
- **列宽调整**: 如果对齐后觉得太挤，可以使用 
    
    width 属性（像素或百分比，但在新版 list view 中通常由浏览器自动计算，XML 中使用 

        <field name="name" width="300px" />

    text-nowrap
    ```
    class="text-start"
    ```

    ```
    class="text-center"
    ```
    

    
    ```
    class="text-end"
    ```
    
设置列表视图可编辑
让“成本计算器”的列表视图支持直接编辑，只需在 list 标签上添加 editable="bottom" 和 multi_edit="1" 属性。

 <list string="报价表" multi_edit="1" editable="bottom">





# 如何让应用默认显示已安装的这个过滤条件的界面

您想要让 Odoo 的“应用”菜单在打开时，默认同时勾选“应用”和“已安装”这两个筛选条件（如截图所示）。
这可以通过修改 Odoo 原生动作 

```
base.open_module_tree
```

 的上下文（Context）来实现。

### 方法说明

我们需要在 XML 中覆盖 

```
base.open_module_tree
```

 这个动作定义的 

```
context
```

 字段，并在其中加入 

```
'search_default_installed': 1
```

。

我将把这段代码添加到您的 

diecut_menu_view.xml 文件中。

Edited

diecut_menu_view.xml

+21-351

我是 **Antigravity**。

要让 Odoo 的“应用”界面默认显示已安装的模块，我为您执行了以下操作：

### 修改说明

我修改了 

diecut_menu_view.xml 文件，在其中添加了一个 XML 记录来覆盖 Odoo 原生动作 

```
base.open_module_tree
```

 的默认行为。

xml

<!-- 修改 Odoo 原生"应用"菜单的默认筛选：默认显示已安装 -->

    <record id="base.open_module_tree" model="ir.actions.act_window">

        <field name="context">{'search_default_app': 1, 'search_default_installed': 1}</field>

    </record>

这段代码的作用是：

1. **
    
    ```
    search_default_app: 1
    ```
    
    **: 保持原来的逻辑，默认筛选“应用”（不显示底层模块）。
2. **
    
    ```
    search_default_installed: 1
    ```
    
    **: **新增逻辑**，默认勾选“已安装”筛选器。

### 接下来

为了使更改生效，请您运行升级模块命令：

powershell

docker-compose exec web odoo -d odoo -u diecut --stop-after-init

升级完成后刷新页面，再次点击“应用”菜单，您应该会看到默认已经勾选了“已安装”。

### 增加切换到表单视图按钮


已为您在“成本计算器”的列表视图最右侧增加了一个“打开详情”的按钮（图标为外部链接风格）。
修改内容
1. 后端逻辑 ( 
diecut_quote.py
)：增加了  
 `def action_open_form(self):`
        `self.ensure_one()`
        `return {`
            `'type': 'ir.actions.act_window',`
            `'res_model': 'diecut.quote',`
            `'res_id': self.id,`
            `'view_mode': 'form',`
            `'target': 'current',`
        `}`
action_open_form
 方法，用于跳转到当前记录的表单视图。

2. 前端视图 ( 
diecut_quote_views.xml
)：在列表视图中增加了一个按钮列，绑定了上述方法。



    <list string="报价单" decoration-bf="final_unit_price &gt; 0" decoration-info="profit_rate &gt; 0.2" multi_edit="1" editable="bottom">
                <field name="name"/>
                <field name="customer_id"/>
                <field name="project_sn"/>
                <field name="internal_sn"/>
                <field name="quote_date"/>
                <field name="final_unit_price" widget="monetary" options="{'currency_field': 'currency_id'}" decoration-bf="1"/>
                <field name="profit_rate" widget="percentage" decoration-success="profit_rate &gt; 0.15" decoration-danger="profit_rate &lt; 0.05"/>
                <field name="user_id" widget="many2one_avatar_user"/>
                <field name="create_date" string="创建时间" optional="hide"/>
                <button name="action_open_form" type="object" icon="fa-external-link" string=" " title="打开详情"/>
            </list>
        </field> 

